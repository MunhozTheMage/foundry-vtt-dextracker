const MODULE_LOCATION = "modules/foundry-vtt-dextracker";

const TEMPLATES_LOCATION = `${MODULE_LOCATION}/templates`;

export const PATHS = {
  TEMPLATES: {
    DEX: `${TEMPLATES_LOCATION}/dex.hbs`,
    SETTINGS: `${TEMPLATES_LOCATION}/settings.hbs`,
  },
};
